# Backend API base URL
VITE_API_URL=http://localhost:5000

# Set to "true" to use mock data without a running backend (local dev only)
# Never set to "true" in production
VITE_USE_MOCK_DATA=false
VITE_USE_MOCK_LMS=false
