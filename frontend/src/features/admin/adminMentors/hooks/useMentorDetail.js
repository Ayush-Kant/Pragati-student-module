// Re-export the canonical implementation to avoid duplicate stubs.
// Always import useMentorDetail from this path or from hooks/useMentorDetail.js —
// both will now resolve to the same full implementation.
export { default } from "../../hooks/useMentorDetail";