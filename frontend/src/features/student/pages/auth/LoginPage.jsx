// LoginPage.jsx
// Purpose: Student login page with email/password form wired to Firebase authentication flow (SM-01)

const LoginPage = () => {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold text-gray-800">LoginPage</h1>
      <p className="text-gray-500 mt-2">Student login page with email/password form wired to Firebase authentication flow (SM-01)</p>
    </div>
  );
};

export default LoginPage;
